{
    "client": {
        "id": "apply your id here from google oauth",
        "secret": "apply your secret key here from oauth",
        "redirect": "http://localhost:5000/auth/google/cb"
    },

    // credentials format applied

    "credentials": {
        "access_token": "your access_token",
        "token_type": "Bearer",
        "expires_in": 2600,
        "refresh_token": "your refresh_token"
    }
}
